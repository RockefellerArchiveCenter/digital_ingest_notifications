[tox]
envlist = py311, linting
skipsdist = True

[testenv]
skip_install = True
deps =
  -rrequirements.txt
  -rtest_requirements.txt

[testenv:py311]
setenv =
  AWS_ROLE_ARN=arn:aws:iam::123456789012:role/digital-ingest-role
commands =
	coverage run -m pytest -s
	coverage report -m

[testenv:linting]
basepython = python3
deps = pre-commit
commands =
    pre-commit run --all-files

[flake8]
application-import-names = flake8
select = B, C, E, F, W, B950
import-order-style = pep8
max-complexity = 10
ignore =
  # Line too long (82 > 79 characters).
	E501,
  # Blank line at end of file
	W391,